<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Swift Wars: iOS 開發之旅</title>
    <style>
        /* --- 全局設定 --- */
        body {
            margin: 0;
            padding: 0;
            width: 100%;
            height: 100%;
            background: #000;
            overflow: hidden; /* 隱藏捲軸，因為內容會超出邊界 */
            font-family: "Franklin Gothic Medium", "Arial Narrow", Arial, sans-serif;
        }

        /* --- 星空背景 (使用 CSS 漸層模擬) --- */
        .stars {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background: #000 url('data:image/svg+xml;utf8,<svg xmlns="